These are the files created to run and operate the StatTracker program in C#

In order to run these, I had to use my terminal using 
cd ~/Desktop/Prog_Lang/Project_Code/StatTrackerRun
dotnet build
dotnet run

It would not run in VS code similar to the C++ and C languages in general and possibly Java. 
This would make sense due to being apart of the C-language family 
